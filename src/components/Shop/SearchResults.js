import'./Products.css';
import ProductsList from './ProductsList';

const SearchResults = () => {
    return (
        <section className="products">
            <ProductsList />
        </section>
    );
};

export default SearchResults;