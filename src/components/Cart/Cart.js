import Card from '../UI/Card';
import './Cart.css';

const Cart = (props) => {
  return (
    <Card className="cart">
      <h2>Your Shopping Cart</h2>
      <ul>
        
      </ul>
      <button>Order</button>
    </Card>
  );
};

export default Cart;
